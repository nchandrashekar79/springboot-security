# Consuming-application configuration. Only the secret and the open paths are set; everything
# else relies on library defaults.
app.security.jwt.secret=a-secret-value-that-is-long-enough-for-hs256-1234567890
app.security.permit-paths=/open
